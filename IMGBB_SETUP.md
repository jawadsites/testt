# إعداد ImgBB لتخزين الصور

## لماذا ImgBB؟
على Render، نظام الملفات مؤقت (ephemeral)، مما يعني أن الملفات المرفوعة تُحذف عند إعادة تشغيل الخادم. لذلك نستخدم ImgBB كحل تخزين سحابي دائم ومجاني **يعمل في جميع الدول**.

## مميزات ImgBB ✨
- ✅ **مجاني تماماً** - بدون حدود على عدد الصور
- ✅ **يعمل عالمياً** - متاح في جميع البلدان
- ✅ **32MB لكل صورة** - حجم كافي للصور عالية الجودة
- ✅ **سريع وموثوق** - CDN عالمي
- ✅ **بدون تسجيل بطاقة ائتمان**

## خطوات الإعداد

### 1️⃣ لديك مفتاح API بالفعل! ✅
```
d6fbe51b65d48fa3ca367be300860ff8
```

### 2️⃣ إضافة المفتاح في Render

1. اذهب إلى https://dashboard.render.com
2. اختر خدمة `postmaker-ai-backend`
3. اذهب إلى تبويب **Environment**
4. أضف متغير جديد:
   ```
   IMGBB_API_KEY = d6fbe51b65d48fa3ca367be300860ff8
   ```
5. احفظ التغييرات (Save Changes)
6. سيتم إعادة تشغيل الخادم تلقائياً (1-2 دقيقة)

### 3️⃣ التحقق من عمل ImgBB

بعد إعادة التشغيل:
1. اذهب إلى: https://postmaker-ai-frontend.onrender.com/admin/login.html
2. سجّل دخول بـ:
   - **اسم المستخدم**: admin
   - **كلمة المرور**: admin123
3. أضف قالب جديد مع صورة
4. ستُرفع الصورة إلى ImgBB تلقائياً! 🎉

## كيف يعمل النظام؟

### الاستراتيجية المتدرجة:
1. **ImgBB أولاً**: إذا كان المفتاح موجود، يتم الرفع إلى ImgBB
2. **Base64 كنسخة احتياطية**: إذا فشل ImgBB، يتم تخزين الصورة في JSON

### ملاحظات هامة:
- 📤 **رفع الصور**: حتى 32MB لكل صورة
- 🔄 **حذف تلقائي**: يمكن ضبط expiration (اختياري)
- 🌐 **CDN عالمي**: صورك ستُحمّل بسرعة في أي مكان
- 💾 **تخزين دائم**: الصور لن تُحذف عند إعادة تشغيل الخادم

## رسالة السجل في Render

بعد رفع صورة، ستشاهد في Logs:
```
📤 Uploading to ImgBB...
✅ ImgBB upload successful
```

## استكشاف الأخطاء

### المفتاح غير صحيح
إذا رأيت خطأ:
```
❌ ImgBB failed, using fallback...
💾 Using base64 fallback storage
```

تأكد من:
- نسخ المفتاح بشكل صحيح في Render Environment
- عدم وجود مسافات قبل أو بعد المفتاح
- إعادة تشغيل الخادم بعد إضافة المتغير

### الصور لا تظهر
1. افحص Render Logs للتأكد من نجاح الرفع
2. تحقق من أن IMGBB_API_KEY موجود في Environment
3. جرب رفع صورة أصغر (أقل من 5MB)

## بدائل إضافية (إذا احتجت)

### Imgur API (مجاني)
- التسجيل: https://api.imgur.com/oauth2/addclient
- 12,500 رفع يومياً

### Supabase Storage (مجاني)
- التسجيل: https://supabase.com
- 1GB تخزين مجاني

## API Reference - ImgBB

### Endpoint
```
POST https://api.imgbb.com/1/upload?key=YOUR_KEY
```

### Parameters
- **key** (مطلوب): مفتاح API
- **image** (مطلوب): base64 أو binary أو URL
- **name** (اختياري): اسم الملف
- **expiration** (اختياري): حذف بعد X ثانية (60-15552000)

### Response
```json
{
  "data": {
    "url": "https://i.ibb.co/xxxxx/image.jpg",
    "display_url": "https://i.ibb.co/xxxxx/image.jpg",
    "delete_url": "https://ibb.co/xxxxx/delete-token"
  },
  "success": true,
  "status": 200
}
```

---

✨ **النظام الآن جاهز للعمل مع ImgBB!**

🔒 **تذكير أمني**: لا تضع المفتاح في الكود - استخدم Environment Variables فقط!
