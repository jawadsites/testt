# Poster Generator - Setup Guide

## 🚀 تثبيت المشروع

### المتطلبات
- Node.js (v14 أو أحدث)
- npm أو yarn

### خطوات التثبيت

1. **تثبيت الاعتماديات**
```bash
cd server
npm install
```

2. **إعداد ملف البيئة**
قم بتعديل ملف `.env` في مجلد `server`:
```
PORT=3000
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

3. **تشغيل السيرفر**
```bash
npm start
```

أو للتطوير مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

## 🌐 الوصول للتطبيق

- **الصفحة الرئيسية**: http://localhost:3000
- **لوحة التحكم**: http://localhost:3000/admin/login.html
- **بيانات الدخول الافتراضية**:
  - اسم المستخدم: `admin`
  - كلمة المرور: `admin123`

## 📁 هيكل المشروع

```
تصميم بوستات/
├── server/                 # Backend
│   ├── routes/            # API endpoints
│   │   ├── auth.js       # Authentication
│   │   └── templates.js  # Template management
│   ├── data/             # JSON database
│   │   └── templates.json
│   ├── uploads/          # Uploaded images
│   ├── .env              # Environment variables
│   ├── package.json
│   └── server.js         # Main server file
│
├── admin/                # Admin panel
│   ├── css/
│   │   └── admin.css
│   ├── js/
│   │   ├── login.js
│   │   └── dashboard.js
│   ├── login.html
│   └── dashboard.html
│
├── css/                  # Frontend styles
├── js/                   # Frontend scripts
├── index.html           # Landing page
└── create.html          # Poster creator
```

## 🔐 لوحة التحكم

### المميزات:
- ✅ تسجيل دخول آمن بـ JWT
- ✅ إضافة قوالب جديدة
- ✅ تعديل القوالب الموجودة
- ✅ حذف القوالب
- ✅ رفع صور للقوالب
- ✅ تصنيف القوالب حسب الفئة

### استخدام لوحة التحكم:

1. **تسجيل الدخول**
   - افتح http://localhost:3000/admin/login.html
   - أدخل بيانات الدخول (admin / admin123)

2. **إضافة قالب جديد**
   - اضغط "إضافة قالب جديد"
   - املأ البيانات (الاسم، الفئة، الوصف)
   - ارفع صورة القالب
   - اضغط "حفظ"

3. **تعديل قالب**
   - اضغط "تعديل" على البطاقة المطلوبة
   - عدّل البيانات
   - احفظ التغييرات

4. **حذف قالب**
   - اضغط "حذف" على البطاقة
   - أكد الحذف

## 🔌 API Endpoints

### Authentication
- `POST /api/auth/login` - تسجيل دخول
- `GET /api/auth/verify` - التحقق من التوكن

### Templates (Public)
- `GET /api/templates` - جلب جميع القوالب
- `GET /api/templates/:id` - جلب قالب محدد

### Templates (Admin Only - يتطلب توكن)
- `POST /api/templates` - إضافة قالب جديد
- `PUT /api/templates/:id` - تحديث قالب
- `DELETE /api/templates/:id` - حذف قالب

## 🔧 التخصيص

### تغيير بيانات الأدمن
عدّل في ملف `.env`:
```
ADMIN_USERNAME=your_username
ADMIN_PASSWORD=your_password
```

### تغيير المنفذ
عدّل في ملف `.env`:
```
PORT=5000
```

### تغيير JWT Secret
⚠️ **مهم للأمان في الإنتاج**:
```
JWT_SECRET=your_very_long_random_secret_key_here
```

## 📦 التصدير للإنتاج

1. غيّر بيانات `.env` لبيانات آمنة
2. استخدم قاعدة بيانات حقيقية بدلاً من JSON
3. أضف HTTPS
4. استخدم خدمة لرفع الصور (AWS S3, Cloudinary)

## 🐛 حل المشاكل

**المشكلة**: السيرفر لا يعمل
- تأكد من تثبيت Node.js
- تأكد من تثبيت الاعتماديات: `npm install`
- تحقق من المنفذ 3000 غير مستخدم

**المشكلة**: لا يمكن رفع الصور
- تأكد من وجود مجلد `server/uploads`
- تحقق من صلاحيات الملفات

**المشكلة**: خطأ في تسجيل الدخول
- تأكد من بيانات الدخول في `.env`
- تحقق من JWT_SECRET موجود

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، تواصل معنا!
